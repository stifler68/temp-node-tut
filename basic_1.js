/*
    if we want to access some variable or funtion form other JS file then at the time we use 
    concept of (module.export) every file has its own module and their empty export 
    we just have to put in module export then we can access in anywhere 
*/

// console.log(__dirname);
// console.log(__filename);

// CommonJS, eveery file is module(by default)
// Modules - Encapsulation Code (only share minimum )


    // const name = require("./3-name");
    // // console.log(name);
    // const func_name = require("./4-utils");
    // // console.log(func_name);
    // const data = require("./5-alternative-flavor");
    // // console.log(data);
    // require("./6-mind-grenade"); 

    // func_name("hemant");
    // func_name(name.ravi);
    // func_name(name.sumeet);
